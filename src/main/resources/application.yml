spring:
  jackson:
    time-zone: Asia/Seoul
  # .env import
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:mysql://festamate-database.c7iqy64cixra.ap-northeast-2.rds.amazonaws.com:3306/festamate
    username: admin
    password: '00000000'
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create
        default_batch_fetch_size: 1000